# playbooks/01_build_structured_configs.yml
# Purpose: Runs AVD eos_designs to build structured configuration data.
---
- name: Build AVD structured configurations
  hosts: FABRIC
  gather_facts: false

  tasks:
    - name: Build structured configs with eos_designs
      ansible.builtin.import_role:
        name: arista.avd.eos_designs
