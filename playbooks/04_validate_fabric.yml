# playbooks/04_validate_fabric.yml
# Purpose: Runs AVD eos_validate_state to validate operational fabric state.
---
- name: Validate fabric state
  hosts: FABRIC
  gather_facts: false

  tasks:
    - name: Run AVD fabric validation with eos_validate_state
      ansible.builtin.import_role:
        name: arista.avd.eos_validate_state
